import unittest
import pandas as pd
import numpy as np


class Product:
    def __init__ (self, (str)name, int(price), int(weight),float(flammibility) int(indentifier)):
    self.name = str.name
    self. price = int(10)
    self.weight = int(20)
    self.flammability = float(0.5)
    self.identifier = int(random.randit(1000000,9999999))


class Else(object):
    pass


def Stealability (self):
    ratio= (self.price/self.weight)
    if ratio < 0.5 return "not stealable"
    if ratio > 0.5 & < 1.0 return 'Not so stealable'
    Else return 'very stealable'


def explode(self):
    Blow_up = flammability * weight
    if Blow_up < 10 return 'fizzle'
    if Blow_up >= 10 & < 50 return 'boom'
    Else return BABOOM




class test_product(unittest.TestCase):
    def product(self):
        prod = product('A cool Toy')
    prod.name
    prod.price
    prod.weight
    prod.flammability
    prod.

    prod.Stealability()
    prod.explode()








